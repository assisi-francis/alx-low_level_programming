#include "main.h"

/**
 * _abs - computes absolute value of integrer
 *
 * @n - n is an integer
 *
 * Return 1 positive -1 negative o is 0
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
